{
  "name": "hedera-wallet-connect-typescript-example",
  "version": "1.0.0",
  "description": "A vanilla html/css/typescript side by side example bundled with esbuild.",
  "scripts": {
    "build": "rimraf dist && node scripts/build.mjs"
  },
  "dependencies": {
    "@hashgraph/hedera-wallet-connect": "^1.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
